[pytest]
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
markers =
    phase0: environment and scaffold checks
    phase1: database schema and seed data checks
    phase2: ingestion, cleaning and audit checks
    phase3: feature builder and training matrix checks
